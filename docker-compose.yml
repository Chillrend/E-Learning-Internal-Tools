version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets
      - ./uploads:/app/uploads
    env_file:
      - .env
    restart: unless-stopped
